import { NavLink, Outlet } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
export default function AppLayout(){const {user,logout}=useAuth();return <div className="shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">↗</span><span>equity<span className="accent">.</span></span></div><nav>{[['/dashboard','▦','Overview'],['/market','◫','Markets'],['/portfolio','◈','Portfolio'],['/transactions','↻','Transactions'],['/watchlist','☆','Watchlist']].map(([to,icon,label])=><NavLink key={to} to={to} className={({isActive})=>isActive?'active':''}>{icon}<span>{label}</span></NavLink>)}</nav><div className="side-bottom"><div className="avatar">{user?.name?.[0]?.toUpperCase()}</div><div><strong>{user?.name}</strong><small>₹{Number(user?.virtualCash||0).toLocaleString('en-IN')} cash</small></div></div></aside><main><header><div><p className="eyebrow">PAPER TRADING ACCOUNT</p><h1>Market at a glance <span>✦</span></h1></div><button className="profile" onClick={logout}>Sign out</button></header><Outlet/></main></div>}
