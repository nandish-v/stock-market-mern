import {useEffect,useState} from 'react';import api from '../services/api';
export default function AdminDashboard(){const [data,setData]=useState(null);const [error,setError]=useState('');useEffect(()=>{Promise.all([api.get('/admin/users'),api.get('/admin/stocks'),api.get('/admin/transactions')]).then(([u,s,t])=>setData({users:u.data.data,stocks:s.data.data,transactions:t.data.data})).catch(e=>setError(e.response?.data?.message||'Unable to load admin data'))},[]);if(error)return <section className="empty-panel"><div className="form-error">{error}</div></section>;if(!data)return <section className="empty-panel">Loading admin dashboard…</section>;return <><div className="section-head"><h2>Admin dashboard</h2></div><section className="cards"><article className="market-card"><div className="card-top"><span>USERS</span></div><strong>{data.users.length}</strong></article><article className="market-card"><div className="card-top"><span>STOCKS</span></div><strong>{data.stocks.length}</strong></article><article className="market-card"><div className="card-top"><span>TRANSACTIONS</span></div><strong>{data.transactions.length}</strong></article></section><div className="section-head"><h2>Users</h2></div><div className="table-wrap"><table><thead><tr><th>NAME</th><th>EMAIL</th><th>ROLE</th><th>CASH</th></tr></thead><tbody>{data.users.map(u=><tr key={u._id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>₹{u.virtualCash.toLocaleString('en-IN')}</td></tr>)}</tbody></table></div></>}
