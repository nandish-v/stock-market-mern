import { useEffect,useState } from 'react'; import { PieChart,Pie,Cell,Tooltip,ResponsiveContainer,LineChart,Line,XAxis,YAxis,CartesianGrid } from 'recharts'; import api from '../services/api';
const COLORS=['#b6f36b','#68d6c0','#8ca8ff','#f5c56b','#d58cff'];
export default function Dashboard(){const [state,setState]=useState({loading:true,error:'',data:null});useEffect(()=>{api.get('/user/dashboard').then(r=>setState({loading:false,data:r.data.data})).catch(e=>setState({loading:false,error:e.response?.data?.message||'Could not load dashboard'}))},[]);if(state.loading)return <section className="empty-panel">Loading portfolio…</section>;if(state.error)return <section className="empty-panel"><div className="form-error">{state.error}</div></section>;const d=state.data;const allocation=d.holdings.map(h=>({name:h.symbol,value:h.currentValue}));const trend=[{name:'Start',value: d.cash+d.investedAmount},{name:'Now',value:d.totalAccountValue}];return <><section className="hero"><div><p className="eyebrow light">TOTAL ACCOUNT VALUE</p><div className="value">₹{d.totalAccountValue.toLocaleString('en-IN')}</div><div className={d.unrealizedPL>=0?'positive':'negative'}>{d.unrealizedPL>=0?'+':''}₹{d.unrealizedPL.toLocaleString('en-IN')} unrealized P/L</div></div><div className="hero-note"><span className="pulse"/> Live portfolio valuation<br/><small>Cash ₹{d.cash.toLocaleString('en-IN')}</small></div></section><div className="section-head"><h2>Portfolio overview</h2></div><section className="cards"><article className="market-card"><div className="card-top"><span>AVAILABLE CASH</span></div><strong>₹{d.cash.toLocaleString('en-IN')}</strong></article><article className="market-card"><div className="card-top"><span>INVESTED</span></div><strong>₹{d.investedAmount.toLocaleString('en-IN')}</strong></article><article className="market-card"><div className="card-top"><span>POSITIONS</span></div><strong>{d.holdings.length}</strong></article></section><section className="chart-grid"><article className="chart-card"><div className="card-top"><h2>Account value</h2><span>Since start</span></div><ResponsiveContainer width="100%" height={220}><LineChart data={trend}><CartesianGrid stroke="#28343d" vertical={false}/><XAxis dataKey="name" stroke="#71808c"/><YAxis hide/><Tooltip contentStyle={{background:'#151d25',border:'1px solid #30404a'}}/><Line type="monotone" dataKey="value" stroke="#b6f36b" strokeWidth={3} dot={{fill:'#b6f36b'}}/></LineChart></ResponsiveContainer></article><article className="chart-card"><div className="card-top"><h2>Allocation</h2><span>By value</span></div>{allocation.length?<ResponsiveContainer width="100%" height={220}><PieChart><Pie data={allocation} dataKey="value" nameKey="name" innerRadius={55} outerRadius={82} paddingAngle={3}>{allocation.map((_,i)=><Cell key={i} fill={COLORS[i%COLORS.length]}/>)}</Pie><Tooltip contentStyle={{background:'#151d25',border:'1px solid #30404a'}}/></PieChart></ResponsiveContainer>:<div className="chart-empty">No positions yet</div>}</article></section><section className="recent"><div className="section-head"><h2>Recent transactions</h2></div>{d.recentTransactions.length?<div className="table-wrap"><table><thead><tr><th>TYPE</th><th>SYMBOL</th><th>QTY</th><th>PRICE</th><th>TOTAL</th></tr></thead><tbody>{d.recentTransactions.map(t=><tr key={t._id}><td><span className={t.type==='BUY'?'tag-buy':'tag-sell'}>{t.type}</span></td><td>{t.symbol}</td><td>{t.quantity}</td><td>₹{t.price.toLocaleString('en-IN')}</td><td>₹{t.totalAmount.toLocaleString('en-IN')}</td></tr>)}</tbody></table></div>:<div className="empty-panel"><p>No transactions yet. Your first trade will appear here.</p></div>}</section></>}
