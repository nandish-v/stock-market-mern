import { useState } from 'react'; import { useLocation, useNavigate, useSearchParams } from 'react-router-dom'; import { useAuth } from '../context/AuthContext';
export default function AuthPage(){const [params]=useSearchParams();const location=useLocation();const mode=location.pathname==='/register'||params.get('mode')==='register'?'register':'login';const [form,setForm]=useState({name:'',email:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const {login,register}=useAuth();const navigate=useNavigate();const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await (mode==='login'?login(form):register(form));navigate('/dashboard')}catch(err){setError(err.response?.data?.message||'Unable to authenticate')}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-brand"><span className="brand-mark">↗</span> equity<span className="accent">.</span></div><div className="auth-card"><p className="eyebrow">PAPER TRADING PLATFORM</p><h1>{mode==='login'?'Welcome back':'Start trading smarter'}</h1><p className="muted">{mode==='login'?'Sign in to your virtual portfolio.':'Create an account and receive ₹10,00,000 in virtual cash.'}</p><form onSubmit={submit}>{mode==='register'&&<input required placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/>}<input required type="email" placeholder="Email address" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input required minLength="8" type="password" placeholder="Password (8+ characters)" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/>{error&&<div className="form-error">{error}</div>}<button className="primary full" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in →':'Create account →'}</button></form><p className="switch">{mode==='login'?'New to equity?':'Already registered?'} <button onClick={()=>navigate(mode==='login'?'/register':'/login')}>{mode==='login'?'Create account':'Sign in'}</button></p></div></div>}
