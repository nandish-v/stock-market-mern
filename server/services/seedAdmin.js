require('dotenv').config();const mongoose=require('mongoose');const bcrypt=require('bcryptjs');const User=require('../models/User');
(async()=>{try{await mongoose.connect(process.env.MONGODB_URI);const email=process.env.ADMIN_EMAIL||'admin@example.com';const password=process.env.ADMIN_PASSWORD||'ChangeMe123!';const hash=await bcrypt.hash(password,12);await User.findOneAndUpdate({email},{name:'Platform Admin',email,password:hash,role:'admin',virtualCash:1000000},{upsert:true,new:true,setDefaultsOnInsert:true});console.log(`Admin ready: ${email}`);await mongoose.disconnect()}catch(e){console.error(e.message);process.exit(1)}})();
